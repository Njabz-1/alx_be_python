# Assign value to the variable
hours = 2  # Number of hours

# Calculate the number of seconds in the given hours
seconds = hours * 3600  # 3600 seconds per hour

# Print the result
print(f"{hours} hour(s) is {seconds} seconds.")